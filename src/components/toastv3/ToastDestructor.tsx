/*
import React from 'react';

import { useToastContext} from './ToastContext';
import type { ToastType} from './ToastContext'



const ToastDestructor = (selfRef:React.RefObject<ToastType>) => {
  const {handleDec} = useToastContext();
  const toastId = React.useRef(selfRef);
  


}
*/
